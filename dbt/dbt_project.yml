name: 'snowflake_pipeline'
version: '1.0.0'
config-version: 2
profile: 'snowflake_pipeline'

model-paths: ["models"]
snapshot-paths: ["snapshots"]

models:
  snowflake_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
