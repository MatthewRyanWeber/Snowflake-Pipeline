"""Operational modules for the pipeline (deploy, load, validate, benchmark, teardown).

Run any of them as a module, e.g. `python -m scripts.run_sql --dir sql/00_setup`.
Shared CLI conventions live in `scripts._cli`.
"""
